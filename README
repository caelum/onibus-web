# Pra rodar localmente

* sudo npm -g install grunt-cli bower
* npm install
* bower install
* grunt run

# Dependencias de browsers modernos

- ES5: Array#forEach, Node#classList
- CSS calc (Chrome, Firefox, Safari/iOS 6, IE9, Opera/WebKit, BB10)
- position fixed
- overflow:scroll
- setImmediate / requestAnimationFrame
